*+QR_COM        Common blocks for qR
        INCLUDE 'DAT_PAR'
        INTEGER ISTAT,MAXL,ILOC,IHDS,IMAP
        INTEGER IFITS,IEXT
        INTEGER(DAT__SZPTR) IPTR
        INTEGER FITS_LNAM
        PARAMETER (FITS_LNAM=80)
        PARAMETER (MAXL=1000)
        CHARACTER LOCS(MAXL)*(DAT__SZLOC)
        CHARACTER TTYPE(MAXL)*(FITS_LNAM)
        COMMON/QRCOM/ISTAT,ILOC
        COMMON/HDS_COM/LOCS
        COMMON/HDS_PTR/IPTR,IHDS,IMAP
        COMMON/FITS_COM/IFITS,IEXT
        COMMON/FITS_NAMES/TTYPE
